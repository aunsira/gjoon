<h2>Pull request</h2>
<form action='profile' method='POST'>
  Please select repo:
  <select name="repo">
    <% list_repos.each do |repo| %>
      <option value="<%= repo %>"><%= repo%></option>
    <% end %>
  </select>
  <input type="submit">
</form><br />

<% if !repos.nil?%>
<dl>
  <dt>List of pull request in repository : <%= repo_name %></dt>
  <% repos.each do |repo| %>
    <dd><a href="<%=repo['html_url']%>"><%= repo['title'] %></a></dd>
  <% end %>
</dl>
<% end %>
